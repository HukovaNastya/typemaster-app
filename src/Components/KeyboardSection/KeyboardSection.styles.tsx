import { styled } from '@mui/material/styles';
import Container from '@mui/material/Container';
import Grid from '@mui/material/Grid';

export const KeyboardSectionWrapper = styled(Container)({
    marginTop: '100px'
});

export const KeyboardSectionImgWrapper = styled(Grid)({
    marginRight: '30px'
});

